/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["i.pravatar.cc", "picsum.photos", "www.nytimes.com"]
  }
}

module.exports = nextConfig
